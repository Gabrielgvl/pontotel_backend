Return an user key information based on his name
---
tags:
  - quotation
parameters:
  - name: Company Symbol
    in: path
    type: string
    description: company stock market's symbol
responses:
  200:
    description: The bovespa's quotation were successfully retrieved
    schema:
      example:
        quotation:
          data: Example
